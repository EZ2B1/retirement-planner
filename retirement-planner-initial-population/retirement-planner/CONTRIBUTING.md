# Contributing

Use `main` for approved releases, `develop` for integrated work, `feature/<name>` for enhancements, `fix/<name>` for corrections, and `tax-year/<year>` for annual updates.

Every pull request should document affected calculations and worksheets, add or update tests, preserve traceability, update documentation, and avoid committing generated outputs.
